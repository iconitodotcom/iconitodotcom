#create python venv
python -m venv <venv path>

#Create flask project
1.- Create Main container folder
2.- inside main container folder, create app.py
3.- Create folder for each application in project e.g core ( which container webpage for example )
3.1 * core
       * __init__py
       * view.py